#' Palette definitions for redbull package
redbull_palettes <- list(
  `redbull` = redbull_cols(),
  `rbprimary` = redbull_cols("red", "blue", "yellow", "grey"),
  `rbcool` = redbull_cols("blue", "light blue"),
  `rbwarm` = redbull_cols("red", "pink"),
  `rbyellowgrey` = redbull_cols("yellow", "grey"),
  `bloomberg` = bloomberg_cols("blue", "green", "pink", "yellow", "hot pink", "dark blue", "orange", "dark green"),
  `bloombergfull` = bloomberg_cols()
  )
